import classNames from "classnames";
import styles from "./home.module.scss";

export default function Home() {
  return (
    <section>
      <div className={styles.visual}>
        <div className={classNames("inner", styles.inners)}>sdsd</div>
      </div>
    </section>
  );
}
